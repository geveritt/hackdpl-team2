<h1>Listing news_highlights</h1>

<table>
  <tr>
    <th>Title</th>
    <th>Description</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @news_highlights.each do |news_highlight| %>
  <tr>
    <td><%= news_highlight.title %></td>
    <td><%= news_highlight.description %></td>
    <td><%= link_to 'Show', news_highlight %></td>
    <td><%= link_to 'Edit', edit_news_highlight_path(news_highlight) %></td>
    <td><%= link_to 'Destroy', news_highlight, method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New News highlight', new_news_highlight_path %>
